"use client";

// This component is completely disabled
// It's kept for reference but will never display anything

export default function PWAUpdateNotifier() {
  // Always return null to never show the update notification
  return null;
}
